import {Post} from "@/api/types/Post";

export interface Account {
    mailAddress:string,
    firstname:string,
    lastname:string,
    Pseudonym:string,
    ProfilPicUrl:string,
    createdAt:string
}